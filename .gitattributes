# Files excluded from `git archive` (and therefore from the GitHub
# "Source code" tarball attached to releases). Repo clones and
# `go install` still include everything — Go module zips can't be
# filtered.

tapes/         export-ignore
test-data/     export-ignore
.gitattributes export-ignore
